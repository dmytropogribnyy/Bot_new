# Core dependencies
ccxt>=4.0.0
aiohttp>=3.8.0
pydantic>=2.0.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0

# Technical analysis
ta>=0.10.0

# Configuration
pyyaml>=6.0

# Optional: For enhanced logging
python-dotenv>=1.0.0
rich>=13.0.0
colorama>=0.4.6

# Optional file lock support in audit logger (kept in prod since used at runtime when available)
portalocker>=2.8.2
